{
  "name": "jabharn-app",
  "version": "0.1.0",
  "private": true,
  "main": "node_modules/expo/AppEntry.js",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "expo": "~57.0.19",
    "expo-status-bar": "~57.0.1",
    "expo-localization": "~57.0.1",
    "react": "19.2.8",
    "react-native": "0.87.1",
    "@react-native-async-storage/async-storage": "^3.1.1",
    "i18next": "^26.4.1",
    "react-i18next": "^17.0.13",
    "firebase": "^12.18.0",
    "@react-native-google-signin/google-signin": "^16.1.4"
  },
  "devDependencies": {
    "typescript": "~5.9.0",
    "@types/react": "~19.2.18",
    "@babel/core": "^8.0.1"
  },
  "_comments": {
    "typescript-version": "Pinned devDependencies to typescript ~5.9 instead of the newest published 7.x — Expo/Metro's toolchain support for TS7 was still settling as of this scaffold's creation; bump once Expo's docs confirm compatibility.",
    "firebase": "Only actually imported once src/config/featureFlags.ts -> SHARE_ENABLED is true (see src/data/CloudRepository.ts and src/auth/AuthService.ts).",
    "google-signin": "Only needed once you wire up the 'Continue with Google' button — see src/auth/AuthService.ts signInWithGoogle()."
  }
}
